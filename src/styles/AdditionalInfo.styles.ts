import { Box, styled } from "@mui/material";

export const AdditionalInfoContainer = styled(Box)({
  display: "flex",
  alignItems: "center",
  justifyContent: "flex-end",
  width: "100%",
  gap: "1em",
});

export const AdditionalInfoContent = styled(Box)({
  display: "flex",
  alignItems: "center",
  gap: "1em",
});